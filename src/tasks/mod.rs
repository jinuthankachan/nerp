//! Tasks module — home for one-off CLI commands (run via `cargo loco task`).
//!
//! Empty for now. When you add a task, declare it here with `pub mod <name>;`
//! and register it in `App::register_tasks` (src/app.rs).
